# Em dev o front usa o proxy /api -> localhost:3456 (evita CORS)
# Para apontar direto ao backend, descomente:
# VITE_API_URL=http://localhost:3456

# Porta da API (padrão: 3456) - usado em build de produção ou sem proxy
# VITE_API_PORT=3456
